{
  "name": "oficina_botjs",
  "version": "1.0.15",
  "description": "Whatsapp BOT",
  "main": "./dist/main.js",
  "types": "./dist/main.d.ts",
  "scripts": {
    "build": "tsc",
    "pre-publish": "tsc && npm --no-git-tag-version version patch",
    "pre-publish-minor": "tsc && npm --no-git-tag-version version minor",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "prod": "node ./dist/main.js"
  },
  "keywords": [],
  "author": "Helio Silva <tsi.heliosilva@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.17.1"
  },
  "devDependencies": {
    "@types/qrcode-terminal": "^0.12.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.3"
  }
}
