{
  "name": "oficina_botjs",
  "version": "1.1.4",
  "description": "Whatsapp BOT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:tag": "tsc && npm --no-git-tag-version version patch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "prod": "node ./dist/index.js"
  },
  "keywords": [],
  "author": "Helio Silva <tsi.heliosilva@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.18.2"
  },
  "devDependencies": {
    "@types/qrcode-terminal": "^0.12.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.3"
  }
}
